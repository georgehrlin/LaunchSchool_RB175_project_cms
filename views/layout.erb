<!DOCTYPE html>
<html lang='en-US'>
  <head>
    <meta charset='utf-8'>
    <link rel="stylesheet" href="stylesheets/cms.css" type='text/css'>
    <title>CMS</title>
  </head>
  <body>
    <% if session[:message] %>
      <p class="message"><%= session.delete(:message) %></p>
    <% end %>

    <main>
      <%= yield %>
    </main>
  </body>
</html>
